# Host-Specific Hyprland Configuration Modules
# Place host-specific files here named after your machine's hostname (e.g. `whio.lua`).
# These are automatically loaded by `hyprland.lua` on the matching host.
